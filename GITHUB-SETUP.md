# 🚀 Настройка GitHub + автодеплой

## Шаг 1: Настройка Git (ВЫПОЛНИТЕ СЕЙЧАС)

Введите ваши данные для Git:

```powershell
# Замените на ВАШЕ ИМЯ
git config --global user.name "Ваше Имя"

# Замените на ВАШ EMAIL (можно любой)
git config --global user.email "your@email.com"
```

**Пример:**
```powershell
git config --global user.name "Yolo Kukkonen"
git config --global user.email "yolo@example.com"
```

После этого выполните:
```powershell
git commit -m "Initial commit: Lander Virtual Keyboard game with CI/CD setup"
```

---

## Шаг 2: Создание GitHub репозитория

### Вариант A: Через веб-интерфейс (рекомендуется)

1. Откройте https://github.com/new
2. Заполните:
   - **Repository name:** `lander-virtual-keyboard`
   - **Description:** `Multiplayer Lander game with virtual keyboard for mobile`
   - **Visibility:** `Public` или `Private`
   - ⚠️ **НЕ** создавайте README, .gitignore, license (они уже есть)
3. Нажмите **Create repository**

4. Скопируйте URL репозитория (например: `https://github.com/ваш_username/lander-virtual-keyboard.git`)

5. Выполните команды:
```powershell
cd C:\OpenServer\domains\yolo-ide-isogenic-keys-dev.ru

# Добавьте remote (замените URL на ВАШИХ!)
git remote add origin https://github.com/ваш_username/lander-virtual-keyboard.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код
git push -u origin main
```

---

## Шаг 3: Добавление GitHub Secrets

Теперь нужно добавить секретные данные для автодеплоя:

### 3.1. Получите SSH ключ

```powershell
# Покажите содержимое ПРИВАТНОГО ключа
Get-Content "$env:USERPROFILE\.ssh\yc" | Write-Host
```

**Скопируйте ВСЁ** (включая `-----BEGIN ... KEY-----` и `-----END ... KEY-----`)

### 3.2. Получите SSH known_hosts

```powershell
# Подключитесь к серверу и сохраните fingerprint
ssh-keyscan -H 51.250.30.92 | Out-File -FilePath "$env:TEMP\known_hosts.txt" -Encoding ASCII
Get-Content "$env:TEMP\known_hosts.txt"
```

**Скопируйте вывод**

### 3.3. Добавьте Secrets в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

Создайте **4 секрета:**

#### Secret 1: `SSH_PRIVATE_KEY`
- **Name:** `SSH_PRIVATE_KEY`
- **Value:** Содержимое файла `C:\Users\yolokukkonen\.ssh\yc` (весь приватный ключ)

#### Secret 2: `SSH_KNOWN_HOSTS`
- **Name:** `SSH_KNOWN_HOSTS`
- **Value:** Вывод команды `ssh-keyscan -H 51.250.30.92`

#### Secret 3: `SERVER_IP`
- **Name:** `SERVER_IP`
- **Value:** `51.250.30.92`

#### Secret 4: `SERVER_USER`
- **Name:** `SERVER_USER`
- **Value:** `ubuntu`

---

## Шаг 4: Проверка автодеплоя

После добавления secrets, автодеплой запустится автоматически при каждом `git push`.

### Протестируйте вручную:

1. Откройте ваш репозиторий на GitHub
2. Перейдите во вкладку **Actions**
3. Нажмите на workflow **"Deploy to Yandex.Cloud"**
4. Нажмите **"Run workflow"** → выберите ветку `main` → **"Run workflow"**

### Проверьте логи:

- В Actions вы увидите процесс деплоя
- Зелёная галочка = успешный деплой
- Красный крестик = ошибка (см. логи)

---

## Шаг 5: Обычный workflow разработки

Теперь при каждом изменении кода:

```powershell
cd C:\OpenServer\domains\yolo-ide-isogenic-keys-dev.ru

# 1. Проверьте изменения
git status

# 2. Добавьте файлы
git add .

# 3. Создайте коммит
git commit -m "Описание изменений"

# 4. Отправьте на GitHub (автодеплой запустится автоматически!)
git push
```

### Процесс:

```
Local → GitHub → GitHub Actions → Yandex.Cloud Server
```

1. `git push` → отправка кода на GitHub
2. GitHub Actions автоматически запускает workflow
3. Workflow деплоит код на сервер 51.250.30.92
4. Сервер автоматически перезапускается
5. Игра обновлена! 🎉

---

## 🔍 Мониторинг деплоя

### Смотреть логи GitHub Actions:
1. Откройте https://github.com/ваш_username/lander-virtual-keyboard/actions
2. Выберите последний run
3. Смотрите прогресс в реальном времени

### Проверить статус на сервере:
```powershell
.\remote-status.ps1
.\remote-logs.ps1 -Lines 50
```

### Открыть игру после деплоя:
```
http://51.250.30.92/examples/50-lander_virtual_keyboard/
```
⚠️ **Не забудьте очистить кэш:** `Ctrl + Shift + R`

---

## 🚨 Устранение проблем

### Проблема: "Permission denied (publickey)"

**Решение:** Проверьте Secret `SSH_PRIVATE_KEY` - он должен содержать ВЕСЬ приватный ключ:
```
-----BEGIN OPENSSH PRIVATE KEY-----
...весь ключ...
-----END OPENSSH PRIVATE KEY-----
```

### Проблема: "Host key verification failed"

**Решение:** Проверьте Secret `SSH_KNOWN_HOSTS`:
```powershell
ssh-keyscan -H 51.250.30.92
```

### Проблема: Workflow не запускается

**Решение:** 
1. Проверьте что файл `.github/workflows/deploy.yml` существует
2. Убедитесь что вы делаете push в ветку `main` или `master`
3. Проверьте вкладку Actions - не отключена ли она?

### Проблема: Деплой прошёл, но игра не обновилась

**Решение:**
```powershell
# Проверьте логи
.\remote-logs.ps1 -Lines 100

# Перезапустите сервер
.\remote-restart.ps1

# Очистите кэш браузера
# Ctrl + Shift + R
```

---

## 📊 Что включено в CI/CD

✅ **Автоматический деплой** при push в main/master  
✅ **Резервное копирование** перед каждым деплоем  
✅ **Установка зависимостей** (npm ci)  
✅ **Перезапуск сервера** после деплоя  
✅ **Проверка работоспособности** HTTP теста  
✅ **Логи деплоя** в GitHub Actions  

---

## 💡 Дополнительные возможности

### Деплой вручную (без push):

```powershell
.\deploy.ps1
```

### Деплой только определённых файлов:

Отредактируйте `.github/workflows/deploy.yml` и добавьте/уберите файлы из `--exclude`

### Деплой в staging окружение:

Создайте новую ветку `staging` и измените workflow для деплоя на другой сервер при push в `staging`

---

## 🎯 Быстрая справка

| Команда | Описание |
|---------|----------|
| `git status` | Проверить изменения |
| `git add .` | Добавить все файлы |
| `git commit -m "..."` | Создать коммит |
| `git push` | Отправить + автодеплой |
| `git log --oneline` | История коммитов |
| `.\remote-status.ps1` | Статус удалённого сервера |
| `.\remote-logs.ps1` | Логи сервера |

---

## 🔗 Полезные ссылки

- **GitHub Actions документация:** https://docs.github.com/en/actions
- **GitHub Secrets:** https://docs.github.com/en/actions/security-guides/encrypted-secrets
- **Git документация:** https://git-scm.com/doc

---

**Теперь у вас полноценный CI/CD! Просто делайте `git push` и код автоматически обновится на сервере! 🚀**

