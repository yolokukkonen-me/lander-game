# 🚀 Пошаговый Деплой на Yandex.Cloud

## Быстрый старт (5 шагов)

### 📋 Что нужно подготовить:
- Аккаунт в Yandex.Cloud
- Домен (или можно без домена, по IP)
- SSH клиент (встроен в Windows 10+, Linux, Mac)

---

## Шаг 1: Создание ВМ в Yandex.Cloud (5 минут)

### 1.1 Войдите в консоль
Откройте: https://console.cloud.yandex.ru/

### 1.2 Создайте ВМ
1. Compute Cloud → Виртуальные машины → **Создать ВМ**

2. **Параметры ВМ:**
   - **Имя:** lander-game-server
   - **Зона доступности:** ru-central1-a
   - **Платформа:** Intel Ice Lake
   - **vCPU:** 2
   - **RAM:** 2 GB
   - **Диск:** 20 GB (SSD)

3. **Образ:**
   - Операционная система: **Ubuntu 22.04 LTS**

4. **Сеть:**
   - ✅ Публичный IP адрес
   - Выберите существующую сеть или создайте новую

5. **Доступ:**
   - **Логин:** ubuntu (или ваш)
   - **SSH ключ:** Вставьте свой публичный SSH ключ
     - Windows: создайте командой `ssh-keygen` в PowerShell
     - Публичный ключ: `~/.ssh/id_rsa.pub` или `C:\Users\ВАШ_ЮЗЕР\.ssh\id_rsa.pub`

6. **Security Group (важно!):**
   - Создайте или выберите группу с правилами:
     ```
     Входящий трафик:
     - TCP 22   (SSH)
     - TCP 80   (HTTP)
     - TCP 443  (HTTPS)
     ```

7. Нажмите **Создать ВМ**

8. **Запишите IP адрес** вашей ВМ (будет показан после создания)

---

## Шаг 2: Подключение к серверу (1 минута)

### 2.1 Откройте терминал
- **Windows:** PowerShell или cmd
- **Linux/Mac:** Terminal

### 2.2 Подключитесь по SSH
```bash
ssh ubuntu@ВАШ_IP_АДРЕС
```

Замените `ВАШ_IP_АДРЕС` на IP вашей ВМ из Yandex.Cloud.

При первом подключении подтвердите: `yes`

---

## Шаг 3: Загрузка файлов проекта (3 минуты)

### Вариант A: Через SCP (рекомендуется)

**На вашем локальном компьютере** откройте новый терминал и выполните:

```bash
scp -r C:\OpenServer\domains\yolo-ide-isogenic-keys-dev.ru ubuntu@ВАШ_IP_АДРЕС:/tmp/lander
```

Замените пути на ваши. После загрузки вернитесь в SSH сессию и выполните:

```bash
sudo mkdir -p /opt/lander
sudo mv /tmp/lander/* /opt/lander/
sudo chown -R www-data:www-data /opt/lander
```

### Вариант B: Через FileZilla (GUI)

1. Скачайте FileZilla: https://filezilla-project.org/
2. Подключитесь:
   - **Хост:** `sftp://ВАШ_IP_АДРЕС`
   - **Пользователь:** ubuntu
   - **Пароль:** (пустой, используется SSH ключ)
   - **Порт:** 22
3. Загрузите всю папку `yolo-ide-isogenic-keys-dev.ru` в `/home/ubuntu/lander`
4. В SSH выполните:
   ```bash
   sudo mkdir -p /opt/lander
   sudo mv ~/lander/* /opt/lander/
   sudo chown -R www-data:www-data /opt/lander
   ```

### Вариант C: Через Git (если проект в репозитории)

```bash
cd /opt
sudo git clone https://github.com/ВАШ_РЕПОЗИТОРИЙ/lander.git
sudo chown -R www-data:www-data /opt/lander
```

---

## Шаг 4: Запуск автоматического деплоя (10 минут)

### 4.1 Загрузите скрипт деплоя

Файлы уже должны быть в `/opt/lander/`, включая `deploy-setup.sh`.

### 4.2 Сделайте скрипт исполняемым

```bash
cd /opt/lander
sudo chmod +x deploy-setup.sh deploy-ssl.sh
```

### 4.3 Запустите установку

```bash
sudo ./deploy-setup.sh
```

Скрипт спросит ваш домен. Если у вас нет домена, используйте IP адрес.

**Что делает скрипт:**
- ✅ Обновляет систему
- ✅ Устанавливает Node.js 18
- ✅ Устанавливает Nginx
- ✅ Устанавливает npm зависимости
- ✅ Создаёт systemd сервис
- ✅ Исправляет client.js для production
- ✅ Настраивает Nginx (статика + WebSocket прокси)
- ✅ Настраивает firewall

### 4.4 Дождитесь завершения

Скрипт выведет инструкции по настройке DNS и SSL.

---

## Шаг 5: Настройка DNS (опционально, 5 минут)

### Если у вас есть домен:

1. Войдите в панель управления вашего регистратора доменов
2. Создайте A-записи:
   ```
   Тип    Имя    Значение (IP)
   A      @      ВАШ_IP_АДРЕС
   A      www    ВАШ_IP_АДРЕС
   ```

3. Дождитесь распространения DNS (5-30 минут)

4. Проверьте:
   ```bash
   nslookup yourdomain.ru
   ```

### Если домена нет:

Можно использовать игру по IP адресу:
```
http://ВАШ_IP_АДРЕС
```

---

## Шаг 6: Установка SSL (опционально, 3 минуты)

**Только если у вас есть домен и DNS настроен!**

```bash
sudo ./deploy-ssl.sh
```

Скрипт спросит:
- Домен
- Email для уведомлений

После установки игра будет доступна по HTTPS:
```
https://yourdomain.ru
```

---

## ✅ Готово! Проверка

### Откройте игру в браузере:

**С доменом:**
- HTTP: `http://yourdomain.ru`
- HTTPS: `https://yourdomain.ru`

**Без домена:**
- HTTP: `http://ВАШ_IP_АДРЕС`

### Проверка статуса сервера:

```bash
# Статус игрового сервера
sudo systemctl status lander

# Логи в реальном времени
sudo journalctl -u lander -f

# Проверка порта 3000
sudo netstat -tulpn | grep 3000
```

### Что должно работать:
- ✅ Игра загружается
- ✅ Canvas отображается
- ✅ WebSocket подключается (проверьте в F12 → Network → WS)
- ✅ Можно управлять кораблём
- ✅ Multiplayer работает (откройте в 2+ вкладках)

---

## 🔧 Управление сервером

### Команды systemd:
```bash
# Запуск
sudo systemctl start lander

# Остановка
sudo systemctl stop lander

# Перезапуск
sudo systemctl restart lander

# Статус
sudo systemctl status lander

# Логи
sudo journalctl -u lander -n 100
sudo journalctl -u lander -f
```

### Обновление кода:

1. Загрузите новые файлы в `/opt/lander/`
2. Перезапустите сервер:
   ```bash
   sudo systemctl restart lander
   ```
3. Очистите кэш браузера: `Ctrl + Shift + R`

---

## 🐛 Решение проблем

### Сервер не запускается:
```bash
# Смотрим логи
sudo journalctl -u lander -n 50

# Проверяем конфигурацию
cd /opt/lander/server
node ige.js -g ../examples/50-lander_virtual_keyboard
```

### Nginx ошибка:
```bash
# Проверка конфигурации
sudo nginx -t

# Перезагрузка
sudo systemctl reload nginx

# Логи
sudo tail -f /var/log/nginx/lander_error.log
```

### WebSocket не подключается:
```bash
# Проверяем, что сервер слушает порт 3000
sudo netstat -tulpn | grep 3000

# Проверяем статус сервиса
sudo systemctl status lander

# Проверяем логи
sudo journalctl -u lander -f
```

### Игра не открывается:
```bash
# Проверяем firewall
sudo ufw status

# Открываем порты если нужно
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

---

## 📊 Мониторинг

### Ресурсы сервера:
```bash
# CPU и RAM
htop

# Диск
df -h

# Процессы Node.js
ps aux | grep node
```

### Логи:
```bash
# Игровой сервер
sudo journalctl -u lander -f

# Nginx access
sudo tail -f /var/log/nginx/lander_access.log

# Nginx errors
sudo tail -f /var/log/nginx/lander_error.log

# System
sudo dmesg -T
```

---

## 💰 Стоимость

Примерная стоимость в Yandex.Cloud (2 vCPU, 2GB RAM, 20GB SSD):
- **~600-800 ₽/месяц** при постоянной работе
- **~20-30 ₽/день** при тестировании

---

## 📝 Контрольный список

- [ ] ВМ создана в Yandex.Cloud
- [ ] SSH подключение работает
- [ ] Файлы проекта загружены в `/opt/lander/`
- [ ] Скрипт `deploy-setup.sh` выполнен успешно
- [ ] Сервис `lander` запущен (`systemctl status lander`)
- [ ] Nginx работает (`systemctl status nginx`)
- [ ] Порт 3000 слушает (`netstat | grep 3000`)
- [ ] Firewall настроен (22, 80, 443)
- [ ] DNS настроен (если используется домен)
- [ ] SSL установлен (если используется домен)
- [ ] Игра открывается в браузере
- [ ] WebSocket подключается (F12 → Network → WS)
- [ ] Multiplayer работает (2+ игрока)

---

## 🎉 Успешного деплоя!

После выполнения всех шагов ваша игра будет доступна онлайн 24/7!

**Вопросы?** Смотрите:
- `TROUBLESHOOTING.md` — решение проблем
- `README-DEPLOYMENT.md` — детальная инструкция
- `FIXES.md` — известные исправления

