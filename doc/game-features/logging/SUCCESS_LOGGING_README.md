# 📊 Система логирования сегментов доставки (v3.0)

## 🎯 Концепция

Система логирует **непрерывные сегменты успешного геймплея**:
- ✅ Логирование начинается автоматически при старте игры и после каждого респавна (через 3.1 сек)
- ✅ Каждая доставка орба → сохранение сегмента → продолжение записи
- ✅ Множество доставок в одной жизни → множество файлов-сегментов
- ✅ Краш → текущий буфер удаляется, после респавна запись продолжается

**Ключевые особенности:**
- 📼 **Непрерывная запись**: логирование не останавливается между доставками
- 🎬 **Сегменты**: каждая доставка = новый файл (включая финальный кадр с deliveryEvent)
- 💥 **Устойчивость к крашам**: неудачный сегмент удаляется, следующий начинается после респавна
- 🎮 **Гибкость**: игрок может приземлиться ИЛИ полететь за следующим орбом сразу

---

## 🚀 Как это работает

### Автоматическое логирование

**Никаких клавиш не нужно нажимать!** Система работает полностью автоматически:

1. **Старт игры / Респавн** → Логирование начинается через 3.1 сек (после разблокировки управления)
2. **Игра продолжается** → Каждые 3 фрейма записываются данные
3. **Доставка на базу** → Сегмент сохраняется + **финальный кадр** с моментом доставки
4. **Продолжение** → Новый сегмент начинается сразу (буфер очищен, запись продолжается)
5. **ИЛИ крэш** → Текущий буфер удаляется БЕЗ сохранения
6. **Респавн после краша** → Логирование продолжается с нового сегмента

### Что логируется

Каждый кадр записывает:
- Позиция корабля (x, y)
- Скорость (velX, velY)
- Угол и угловая скорость
- Топливо
- Состояние (несет орб или нет)
- Управление (left, right, thrust, drop)
- Расстояния и углы до ближайшего орба/платформы

---

## 📁 Формат файлов

### Имя файла

```
success_segment_<timestamp>_score<очки>_frames<количество>.json
```

**Примеры:**
- `success_segment_1730645123456_score250_frames145.json` - 145 кадров, заработано 250 очков
- `success_segment_1730645234567_score180_frames98.json` - 98 кадров, заработано 180 очков
- `success_segment_1730645345678_score300_frames210.json` - следующий сегмент той же жизни

### Структура файла

```json
{
  "metadata": {
    "timestamp": 1730645123456,
    "date": "2024-11-03T15:25:23.456Z",
    "segmentType": "delivery_segment",
    "scoreEarned": 250,
    "totalFrames": 146,
    "duration": "7.3 seconds",
    "playerId": "player-12345",
    "successful": true,
    "description": "Successful delivery segment ending with orb deposit (ship may land or fly away for next orb)"
  },
  "frames": [
    {
      "x": 1500,
      "y": 300,
      "velX": 1.2,
      "velY": -0.5,
      "angle": 0.1,
      "angVel": 0.02,
      "fuel": 85.5,
      "carryingOrb": true,
      "left": false,
      "right": true,
      "thrust": true,
      "drop": false,
      "orbDist": 0,
      "orbAngle": 0,
      "padDist": 450,
      "padAngle": -0.3
    },
    ... (много кадров) ...,
    {
      // ПОСЛЕДНИЙ КАДР с моментом доставки:
      "x": 400,
      "y": 500,
      "velX": 0.1,
      "velY": -0.2,
      "angle": 0.0,
      "fuel": 75,
      "carryingOrb": false,        // ← Орб доставлен!
      "landed": true,              // или false если полетел дальше
      "deliveryEvent": true,       // ← Маркер события доставки
      "scoreEarned": 250,          // ← Полученные очки
      "padDist": 0,
      "orbDist": 450,
      ...
    }
  ]
}
```

### ⚡ Финальный кадр с deliveryEvent

**Ключевая особенность v3.0:** Последний кадр каждого сегмента помечен специальными полями:

- `deliveryEvent: true` - маркер успешной доставки
- `scoreEarned: 250` - количество очков за доставку
- `carryingOrb: false` - орб уже сброшен
- `landed: true/false` - игрок приземлился или полетел дальше

**Зачем это важно для ML:**
```python
# Четкий сигнал завершения эпизода
for frame in segment['frames']:
    if frame.get('deliveryEvent'):
        reward = frame['scoreEarned']  # Явная награда
        done = True  # Конец сегмента
        
# Можно анализировать стратегию игрока:
if frame['landed']:
    # Игрок предпочитает безопасность (приземление)
else:
    # Игрок играет агрессивно (летит за следующим)
```

---

## 🎮 Процесс игры

### Сценарий 1: Множественные доставки в одной жизни ✅✅✅

```
1. Старт игры → [ЛОГИРОВАНИЕ НАЧАЛОСЬ через 3.1 сек]
2. Игрок летит, берет орб, доставляет → [СЕГМЕНТ 1 СОХРАНЕН]
3. Игрок летит дальше → записываются все действия
4. Берет второй орб, доставляет → [СЕГМЕНТ 2 СОХРАНЕН]
5. Игрок летит за третьим → записываются все действия
6. Берет третий орб, доставляет → [СЕГМЕНТ 3 СОХРАНЕН]
7. Продолжает игру...
```

**Результат:** 3 файла `success_segment_*.json` в папке `logs/`

**Консоль сервера:**
```
[TRAINING] 🎬 Started segment logging at game start (client: ...)
[TRAINING] ✅ Saved DELIVERY SEGMENT: 145 frames, score: 250 -> success_segment_1730645123456_score250_frames145.json
[TRAINING] ✅ Saved DELIVERY SEGMENT: 98 frames, score: 180 -> success_segment_1730645234567_score180_frames98.json
[TRAINING] ✅ Saved DELIVERY SEGMENT: 210 frames, score: 300 -> success_segment_1730645345678_score300_frames210.json
```

---

### Сценарий 2: Доставка + Краш ✅💥

```
1. Старт игры → [ЛОГИРОВАНИЕ НАЧАЛОСЬ]
2. Берет орб, доставляет → [СЕГМЕНТ 1 СОХРАНЕН]
3. Летит за вторым орбом → записываются действия
4. КРАШ! → [БУФЕР УДАЛЕН без сохранения]
5. Респавн → [ЛОГИРОВАНИЕ ПРОДОЛЖАЕТСЯ]
6. Берет орб, доставляет → [СЕГМЕНТ 2 СОХРАНЕН]
```

**Результат:** 2 файла (неудачный сегмент НЕ сохранен)

**Консоль сервера:**
```
[TRAINING] 🎬 Started segment logging at game start (client: ...)
[TRAINING] ✅ Saved DELIVERY SEGMENT: 145 frames, score: 250 -> success_segment_1730645123456_score250_frames145.json
[TRAINING] 💥 Crash detected - discarding 87 log entries (unsuccessful segment)
[TRAINING] ▶️ Continuing segment logging after respawn
[TRAINING] ✅ Saved DELIVERY SEGMENT: 120 frames, score: 200 -> success_segment_1730645456789_score200_frames120.json
```

---

### Сценарий 3: Мастер-выключатель 'L' 🔴

```
1. Игрок играет с активным логированием → ● LOGGING
2. Нажатие 'L' → [ЛОГИРОВАНИЕ ВЫКЛЮЧЕНО]
3. Индикатор меняется на → ● LOGGING OFF (красный)
4. Все доставки НЕ записываются
5. Нажатие 'L' снова → [ЛОГИРОВАНИЕ ВКЛЮЧЕНО]
6. При старте игры или после респавна → логирование начнется автоматически
```

**Консоль сервера:**
```
[TRAINING] ⛔ Logging system DISABLED for player ...
[TRAINING] ✅ Logging system ENABLED for player ...
(после респавна или старта новой игры)
[TRAINING] 🎬 Started segment logging at game start / after respawn
```

---

## 📈 Анализ данных

### Быстрая статистика

```bash
# Подсчет успешных сегментов
ls logs/success_segment_*.json | wc -l

# Общий размер данных
du -sh logs/
```

### Python скрипт для анализа

Используйте готовый скрипт `analyze_successful_deliveries.py`:

```bash
python analyze_successful_deliveries.py
```

**Или вручную:**

```python
import json
import glob
from pathlib import Path

def analyze_segments():
    log_files = glob.glob('logs/success_segment_*.json')
    
    print(f"📊 Анализ успешных доставок")
    print(f"=" * 50)
    print(f"Всего файлов: {len(log_files)}")
    
    total_frames = 0
    total_score = 0
    durations = []
    
    for file in log_files:
        with open(file, 'r') as f:
            data = json.load(f)
            meta = data['metadata']
            
            total_frames += meta['totalFrames']
            total_score += meta['scoreEarned']
            
            # Длительность в секундах
            duration = meta['totalFrames'] * 3 / 60
            durations.append(duration)
    
    if durations:
        avg_duration = sum(durations) / len(durations)
        avg_score = total_score / len(log_files)
        
        print(f"Всего кадров: {total_frames:,}")
        print(f"Всего очков: {total_score:,}")
        print(f"Средняя длительность: {avg_duration:.1f}s")
        print(f"Средний счет: {avg_score:.1f}")
        print(f"Самая быстрая: {min(durations):.1f}s")
        print(f"Самая долгая: {max(durations):.1f}s")
    
    print(f"=" * 50)

if __name__ == '__main__':
    analyze_successful_deliveries()
```

---

## 🎯 Преимущества новой системы

### Для машинного обучения

✅ **Только качественные данные**
- Все примеры - успешные маневры
- Нет "мусорных" данных от крэшей
- Легко фильтровать по качеству (по score)

✅ **Структурированные эпизоды**
- Каждый файл = один полный цикл
- Четкое начало (pickup) и конец (deposit)
- Легко батчить для обучения

✅ **Метаданные для анализа**
- Быстро найти лучшие примеры (по score)
- Фильтровать по длительности
- Сортировать по сложности

### Для разработки

✅ **Автоматизация**
- Не нужно помнить про клавишу L
- Не нужно вручную останавливать логирование
- Работает в фоне

✅ **Чистота данных**
- Только релевантные данные
- Нет незавершенных сессий
- Четкая атрибуция (какой файл = какая доставка)

---

## 🔧 Настройки

### Изменить интервал логирования

В `Player.js`:
```javascript
self._logInterval = 3; // Логировать каждые 3 фрейма (~50ms при 60fps)
```

**Рекомендации:**
- `3` - хороший баланс (20 записей/сек)
- `1` - максимальная детализация (60 записей/сек, большие файлы)
- `5` - меньше данных (12 записей/сек)

### Добавить дополнительные поля

В методе `_logPlayerState()` в `Player.js`:
```javascript
var logEntry = {
    // ... существующие поля
    altitude: this.getAltitude(),  // Добавить высоту
    nearestPadId: nearestPad ? nearestPad.id : null  // ID платформы
};
```

---

## 📊 Целевые показатели для обучения

### Минимум для Behavioral Cloning
- **20-30 успешных доставок** (файлов)
- **~2000-3000 кадров** всего
- **~2-3 минуты игрового времени**

### Оптимально для качественной модели
- **100+ успешных доставок**
- **10,000+ кадров**
- **~10 минут игрового времени**

### Для продвинутого обучения
- **500+ успешных доставок**
- **50,000+ кадров**
- **Разные платформы, разные дистанции**

---

## ⚡ Quick Start

1. **Запустите игру:**
   ```bash
   .\restart-local.ps1
   ```

2. **Играйте нормально:**
   - Берите орбы
   - Доставляйте на платформы
   - Система логирует автоматически

3. **Проверьте результаты:**
   ```bash
   ls logs/success_delivery_*.json
   ```

4. **Проанализируйте:**
   ```bash
   python ml_bot/scripts/analyze_successful_deliveries.py
   ```

---

## 🐛 Устранение неполадок

### Файлы не создаются

**Проверьте консоль сервера:**
- Видите `[TRAINING] Started automatic logging...`?
- Видите `[TRAINING] ✅ Saved SUCCESSFUL delivery...`?

**Если нет:**
- Убедитесь что вы не бот (`_isBot = false`)
- Проверьте что орб был успешно доставлен
- Проверьте папку `logs/` существует

### Слишком большие файлы

Уменьшите `_logInterval` в `Player.js`:
```javascript
self._logInterval = 5; // Было 3, стало 5
```

### Не хватает данных

Просто играйте больше! 😊 Каждая успешная доставка = новый файл.

---

## 📝 Changelog

**v2.0 (2024-11-03)**
- ✅ Автоматическое начало логирования при подъеме орба
- ✅ Автоматическое сохранение при успешной доставке
- ✅ Автоматическое удаление при крэше
- ✅ Новый формат имени файла с метриками
- ✅ Метаданные в каждом файле
- ✅ Убрано ручное управление (клавиша L теперь не нужна)

**v1.0 (ранее)**
- Ручное логирование по клавише L
- Автосохранение каждые 1000 записей
- Смешанные данные (успешные + неудачные)

---

**Удачи в сборе данных! 🎮🤖**

