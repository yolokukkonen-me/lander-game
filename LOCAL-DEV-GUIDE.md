# 🖥️ Локальная разработка - Руководство

## 🚀 Быстрый старт

### Запустить сервер
```powershell
.\start-local.ps1
```
- Автоматически откроет браузер
- Игра доступна: http://localhost:3000/examples/50-lander_virtual_keyboard/

### Остановить сервер
```powershell
.\stop-local.ps1
```

### Перезапустить сервер
```powershell
.\restart-local.ps1
```

### Проверить статус
```powershell
.\status-local.ps1
```

---

## 📋 Доступные команды

| Команда | Описание |
|---------|----------|
| `.\start-local.ps1` | Запустить локальный сервер |
| `.\stop-local.ps1` | Остановить сервер |
| `.\restart-local.ps1` | Перезапустить сервер |
| `.\status-local.ps1` | Показать статус сервера |
| `.\logs-local.ps1` | Информация о логах |
| `.\deploy.ps1` | Деплой на Yandex.Cloud |

---

## 🔧 Процесс разработки

### 1. Запустить локальный сервер
```powershell
.\start-local.ps1
```

### 2. Внести изменения в код
Редактируйте файлы в:
- `examples/50-lander_virtual_keyboard/` - клиентская логика
- `examples/50-lander_virtual_keyboard/gameClasses/` - игровые классы
- `server/` - серверная логика (редко)

### 3. Перезапустить для применения изменений
```powershell
.\restart-local.ps1
```

### 4. Проверить в браузере
- Откройте: http://localhost:3000/examples/50-lander_virtual_keyboard/
- **Жёсткая перезагрузка:** `Ctrl+Shift+R`

### 5. Задеплоить на сервер
```powershell
.\deploy.ps1
```

---

## 🐛 Устранение проблем

### Ошибка "Port 3000 already in use"
```powershell
# Остановите существующий процесс
.\stop-local.ps1

# Или запустите на другом порту
.\start-local.ps1 -Port 3001
```

### Сервер не запускается
```powershell
# Проверьте Node.js
node --version

# Переустановите зависимости
cd server
npm install
cd ..

# Попробуйте снова
.\start-local.ps1
```

### Изменения не применяются
```powershell
# Жёсткий перезапуск
.\stop-local.ps1
.\start-local.ps1

# Очистите кэш браузера
# Chrome/Edge: Ctrl+Shift+Delete
# Или жёсткая перезагрузка: Ctrl+Shift+R
```

### Как найти логи
Логи отображаются в терминале, где запущен сервер. Если запускали через `start-local.ps1`, проверьте открывшееся окно Node.js.

Для сохранения логов в файл:
```powershell
cd server
node ige.js -g ../examples/50-lander_virtual_keyboard > ../server.log 2>&1
```

---

## 🎯 Типичные сценарии

### Сценарий 1: Быстрое тестирование изменений
```powershell
# 1. Измените код
# 2. Перезапустите
.\restart-local.ps1
# 3. Обновите браузер (Ctrl+Shift+R)
```

### Сценарий 2: Работа с несколькими версиями
```powershell
# Запустите dev версию на порту 3000
.\start-local.ps1

# Запустите prod версию на порту 3001
cd ..\yolo-ide-isogenic-keys.ru
.\start-local.ps1 -Port 3001
```

### Сценарий 3: Деплой после тестирования
```powershell
# 1. Проверьте локально
.\start-local.ps1

# 2. Протестируйте игру
# http://localhost:3000/examples/50-lander_virtual_keyboard/

# 3. Остановите локальный сервер
.\stop-local.ps1

# 4. Задеплойте на прод
.\deploy.ps1
```

---

## 📂 Структура проекта

```
yolo-ide-isogenic-keys-dev.ru/
├── start-local.ps1       # Запуск локального сервера
├── stop-local.ps1        # Остановка сервера
├── restart-local.ps1     # Перезапуск сервера
├── status-local.ps1      # Статус сервера
├── logs-local.ps1        # Информация о логах
├── deploy.ps1            # Деплой на Yandex.Cloud
├── .server-process.json  # Информация о запущенном процессе (авто)
├── server/               # Node.js сервер
│   ├── ige.js           # Точка входа
│   ├── package.json     # Зависимости
│   └── node_modules/    # Библиотеки
├── engine/               # Isogenic Engine
└── examples/
    └── 50-lander_virtual_keyboard/
        ├── client.js            # Клиентская логика
        ├── server.js            # Серверная логика игры
        ├── index.html           # HTML страница
        ├── virtualjoystick.js   # Виртуальная клавиатура
        └── gameClasses/         # Игровые классы
            ├── Player.js
            ├── Orb.js
            ├── ClientWorld.js
            └── ...
```

---

## 🔄 Workflow разработки

### Разработка новой функции

```powershell
# 1. Запустите локальный сервер
.\start-local.ps1

# 2. Откройте файл для редактирования
code examples\50-lander_virtual_keyboard\gameClasses\Player.js

# 3. Внесите изменения

# 4. Перезапустите сервер
.\restart-local.ps1

# 5. Проверьте в браузере (Ctrl+Shift+R)

# 6. Повторяйте шаги 3-5 до достижения результата

# 7. Деплой на прод
.\deploy.ps1
```

### Отладка проблемы

```powershell
# 1. Проверьте статус
.\status-local.ps1

# 2. Проверьте логи
.\logs-local.ps1

# 3. Перезапустите
.\restart-local.ps1

# 4. Если не помогло - остановите и запустите заново
.\stop-local.ps1
.\start-local.ps1
```

---

## 💡 Полезные советы

### Горячая перезагрузка
К сожалению, Isogenic Engine не поддерживает hot reload. Нужно перезапускать сервер после каждого изменения.

### Браузер кэширует JS файлы
Всегда используйте **Ctrl+Shift+R** для жёсткой перезагрузки страницы.

### Несколько экземпляров
Можно запустить несколько серверов на разных портах для тестирования:
```powershell
.\start-local.ps1 -Port 3000
.\start-local.ps1 -Port 3001
.\start-local.ps1 -Port 3002
```

### Автоматический деплой
После тестирования локально - один скрипт деплоит на прод:
```powershell
.\deploy.ps1
```

### Работа с Git
```powershell
# Сохраните изменения
git add .
git commit -m "Added new feature"

# Деплой (если настроен GitHub Actions)
git push
```

---

## 📊 Мониторинг

### Проверка работы сервера
```powershell
# Статус процесса
.\status-local.ps1

# Проверка порта
Test-NetConnection -ComputerName localhost -Port 3000

# Проверка через браузер
start http://localhost:3000/examples/50-lander_virtual_keyboard/
```

### Производительность
Статус покажет:
- CPU usage
- Memory usage
- Uptime

```powershell
.\status-local.ps1
```

---

## 🔗 Полезные ссылки

- **Локальная игра:** http://localhost:3000/examples/50-lander_virtual_keyboard/
- **Продакшн:** http://51.250.30.92/examples/50-lander_virtual_keyboard/
- **CI/CD инструкция:** [CI-CD-SETUP.md](CI-CD-SETUP.md)
- **Быстрый старт деплоя:** [QUICK-START.md](QUICK-START.md)
- **Основная документация:** [README.md](README.md)

---

**Удачной разработки! 🎮🚀**

