# Training Logs Management Guide

Полное руководство по работе с тренировочными данными (логами игры) на удаленном сервере.

---

## 📁 Где хранятся логи?

**На сервере:**
```
/opt/lander/examples/50-lander_virtual_keyboard/logs/
```

**Формат файлов:**
```
success_segment_TIMESTAMP_scoreN_framesM.json
```

**Пример:**
```
success_segment_1730523456789_score10_frames150.json
success_segment_1730523500123_score10_frames200.json
```

---

## 🔍 Просмотр логов на сервере

### Быстрый просмотр (PowerShell)

```powershell
.\remote-logs-view.ps1
```

**Что показывает:**
- Общее количество сегментов
- Общий размер
- Самый старый и новый файл
- Последние 15 файлов с датами

**Детальный просмотр:**
```powershell
.\remote-logs-view.ps1 -Detailed
```
Позволяет просмотреть содержимое конкретного файла.

### Через SSH (ручной способ)

**Подключиться к серверу:**
```powershell
.\remote-ssh.ps1
```

**Посмотреть логи:**
```bash
cd /opt/lander/examples/50-lander_virtual_keyboard/logs
ls -lht success_segment_*.json | head -20    # Последние 20 файлов
ls success_segment_*.json | wc -l            # Количество файлов
du -sh .                                     # Общий размер
```

**Просмотреть содержимое:**
```bash
cat success_segment_1730523456789_score10_frames150.json | head -50
```

---

## ⬇️ Скачивание логов

### Скачать все логи

```powershell
.\remote-logs-download.ps1
```

Логи будут скачаны в папку `downloaded_logs/`

### Скачать последние N файлов

```powershell
# Скачать последние 50 файлов
.\remote-logs-download.ps1 -LastN 50

# Скачать последние 100 файлов
.\remote-logs-download.ps1 -LastN 100
```

### Скачать за период

```powershell
# Скачать логи за последние 7 дней
.\remote-logs-download.ps1 -LastDays 7

# Скачать логи за последние 30 дней
.\remote-logs-download.ps1 -LastDays 30
```

### Указать папку для скачивания

```powershell
.\remote-logs-download.ps1 -OutputDir "C:\MyLogs\batch1"
```

### Через scp (ручной способ)

```bash
scp -i ~/.ssh/yc ubuntu@51.250.30.92:/opt/lander/examples/50-lander_virtual_keyboard/logs/success_segment_*.json ./downloaded_logs/
```

---

## 📊 Анализ скачанных логов

После скачивания логов используйте Python скрипт для анализа:

```bash
cd examples/50-lander_virtual_keyboard
python analyze_successful_deliveries.py
```

**Что показывает:**
- Количество сегментов
- Общее количество кадров
- Средняя длительность сегмента
- Средний счет за доставку
- Статистика по способам доставки (landed vs flying)
- Рекомендации по обучению

**Экспорт объединенного датасета:**
```bash
python analyze_successful_deliveries.py --export
```

Создаст файл `combined_successful_segments.json` для ML обучения.

---

## 🧹 Очистка старых логов

### ⚠️ ВАЖНО

**Перед удалением:**
1. Скачайте логи локально
2. Или создайте архив (используйте флаг `-Archive`)

### Удалить логи старше 30 дней

```powershell
.\remote-logs-clean.ps1 -OlderThanDays 30
```

### Создать архив перед удалением

```powershell
.\remote-logs-clean.ps1 -OlderThanDays 30 -Archive
```

Архив будет создан в `/tmp/logs_archive_YYYYMMDD_HHMMSS.tar.gz` на сервере.

**Скачать архив:**
```powershell
scp -i ~/.ssh/yc ubuntu@51.250.30.92:/tmp/logs_archive_*.tar.gz .
```

### Удалить без подтверждения (осторожно!)

```powershell
.\remote-logs-clean.ps1 -OlderThanDays 7 -Force
```

---

## 🎯 Типичные сценарии

### Сценарий 1: Сбор данных для первого обучения

**Цель:** Собрать 50-200 сегментов для BC обучения

```powershell
# 1. Проверить количество логов
.\remote-logs-view.ps1

# 2. Если >= 50 сегментов - скачать
.\remote-logs-download.ps1

# 3. Анализ
cd examples\50-lander_virtual_keyboard
python analyze_successful_deliveries.py

# 4. Если данных достаточно - начать обучение
cd ..\..\ml_bot
python training\train_bc.py
```

### Сценарий 2: Регулярная очистка сервера

**Цель:** Не заполнять диск сервера

```powershell
# Каждые 2 недели:

# 1. Скачать все логи
.\remote-logs-download.ps1 -OutputDir ".\logs_backup_$(Get-Date -Format 'yyyy-MM-dd')"

# 2. Удалить логи старше 14 дней
.\remote-logs-clean.ps1 -OlderThanDays 14
```

### Сценарий 3: Сбор свежих данных для дообучения

**Цель:** Получить новые логи после изменений в игре

```powershell
# Скачать только последние 7 дней
.\remote-logs-download.ps1 -LastDays 7 -OutputDir ".\new_data"

# Обучить новую модель
cd ml_bot
python training\train_bc.py --logs-dir ..\new_data
```

### Сценарий 4: Быстрая проверка прогресса

**Цель:** Проверить сколько данных собрано

```powershell
# Быстрый просмотр
.\remote-logs-view.ps1

# Результат покажет:
# Total segments: 157
# Total size: 8.2M
# Oldest: success_segment_1730000000_score10_frames120.json
# Newest: success_segment_1730999999_score10_frames180.json
```

---

## 🔧 Структура лог-файла (v3.0)

```json
{
  "metadata": {
    "timestamp": 1730523456789,
    "date": "2025-11-04T10:30:56.789Z",
    "segmentType": "delivery_segment",
    "scoreEarned": 10,
    "totalFrames": 150,
    "duration": "7.5 seconds",
    "playerId": "player1",
    "successful": true,
    "description": "Successful delivery segment ending with orb deposit"
  },
  "frames": [
    {
      "time": 1730523450000,
      "x": 800,
      "y": 500,
      "angle": 0.1,
      "velX": 0.5,
      "velY": -0.2,
      "angVel": 0.01,
      "fuel": 95,
      "score": 0,
      "carryingOrb": false,
      "landed": false,
      "left": false,
      "right": false,
      "thrust": true,
      "drop": false,
      "orbDist": 150,
      "orbAngle": 0.5,
      "orbX": 750,
      "orbY": 450,
      "padDist": 500,
      "padAngle": -1.2,
      "padX": 800,
      "padY": 100
    },
    // ... many more frames ...
    {
      // FINAL FRAME - момент доставки
      "time": 1730523456789,
      "deliveryEvent": true,  // <-- Маркер доставки
      "scoreEarned": 10,      // <-- Полученные очки
      // ... остальные поля
    }
  ]
}
```

---

## 📈 Мониторинг сбора данных

### Рекомендуемое количество данных

**Для Behavioral Cloning (BC):**
- Минимум: 50 сегментов (~2,000-5,000 кадров)
- Рекомендуется: 100-200 сегментов (~5,000-15,000 кадров)
- Оптимально: 500+ сегментов (20,000+ кадров)

**Для PPO (Reinforcement Learning):**
- Не требует логов игрока
- Обучается в симуляции

### Как собрать больше данных?

1. **Играть дольше** - каждая успешная доставка = 1 сегмент
2. **Включить логирование** - клавиша `L` в игре
3. **Несколько игроков** - логи от всех игроков копятся
4. **Разные стратегии** - разнообразие улучшает обучение

### Проверка качества данных

```bash
python analyze_successful_deliveries.py
```

**Смотреть на:**
- Success rate - должно быть 100% (только успешные сегменты)
- Average frames - оптимально 100-300 кадров на сегмент
- Variety - смесь landed/flying deliveries

---

## 🚨 Troubleshooting

### Проблема: "LOGS_DIR_NOT_FOUND"

**Причина:** Папка logs еще не создана на сервере.

**Решение:**
1. Запустите игру на сервере
2. Сделайте хотя бы одну успешную доставку
3. Папка создастся автоматически

### Проблема: Логи не создаются

**Проверить:**
1. Логирование включено? (клавиша `L` - должен быть индикатор "● LOGGING")
2. Доставка успешна? (краши не сохраняются)
3. Права на запись в папку logs?

**SSH проверка:**
```bash
cd /opt/lander/examples/50-lander_virtual_keyboard
ls -la | grep logs
# Должна быть папка logs с правами rwx
```

### Проблема: SSH key not found

**Решение:**
Укажите путь к ключу явно:
```powershell
.\remote-logs-view.ps1 -KeyPath "C:\Path\To\Your\key"
```

### Проблема: Мало места на сервере

**Проверить место:**
```bash
df -h /opt/lander
```

**Очистить:**
```powershell
# Скачать все логи
.\remote-logs-download.ps1

# Удалить старые
.\remote-logs-clean.ps1 -OlderThanDays 7
```

---

## 📚 Связанные документы

- **QUICK-START.md** - Быстрый старт проекта
- **REMOTE-MANAGEMENT.md** - Управление удаленным сервером
- **ml_bot/TRAINING_GUIDE.md** - Руководство по обучению ML бота
- **examples/50-lander_virtual_keyboard/README.md** - Документация игры

---

## 💡 Best Practices

1. **Регулярно скачивайте логи** - не храните все на сервере
2. **Делайте бэкапы** - перед очисткой всегда архивируйте
3. **Анализируйте перед обучением** - проверяйте качество данных
4. **Версионируйте датасеты** - называйте папки по датам
5. **Документируйте изменения** - если изменили игровую логику, пометьте датасет

---

## 🎓 Примеры команд

```powershell
# Быстрая проверка
.\remote-logs-view.ps1

# Скачать все
.\remote-logs-download.ps1

# Скачать последние 50
.\remote-logs-download.ps1 -LastN 50

# Очистить старые (с архивом)
.\remote-logs-clean.ps1 -OlderThanDays 30 -Archive

# Анализ
python analyze_successful_deliveries.py

# Обучение BC
cd ml_bot
python training\train_bc.py
```

---

**Версия:** 3.0  
**Дата:** 2025-11-04  
**Автор:** Yolo Lander Team

