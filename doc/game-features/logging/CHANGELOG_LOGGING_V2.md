# 📝 Changelog: Система логирования v2.0

## Дата: 2024-11-03
## Версия: 2.0 - Автоматическое логирование успешных доставок

---

## 🎯 Главные изменения

### ✅ Автоматизация логирования

**Было (v1.0):**
- Нужно вручную нажимать клавишу `L` для начала/остановки логирования
- Логи сохранялись каждые 1000 записей
- В логах были и успешные, и неудачные попытки

**Стало (v2.0):**
- 🚀 Логирование начинается **автоматически** при подъеме орба
- ✅ Логи сохраняются **автоматически** при успешной доставке на базу
- ❌ Логи удаляются **автоматически** при крэше (без сохранения)
- 🎯 Только успешные доставки = только качественные данные

---

## 📄 Измененные файлы

### 1. `gameClasses/Player.js`

#### Добавлено:
- **Метод `_saveSuccessfulDeliveryLog(scoreEarned)`** - сохранение логов успешной доставки с метаданными
- **Автоматический старт логирования** в методе `carryOrb()` при подъеме орба
- **Автоматическая очистка логов** в методе `crash()` при крэше

#### Изменено:
- **`carryOrb()`**: добавлено автоматическое начало логирования
- **`crash()`**: добавлена очистка логов без сохранения
- **`_logPlayerState()`**: убрано автосохранение каждые 1000 записей

### 2. `gameClasses/Orb.js`

#### Изменено:
- **`deposit()`**: добавлен вызов `player._saveSuccessfulDeliveryLog()` при успешной доставке

### 3. Новые файлы

#### Документация:
- **`SUCCESS_LOGGING_README.md`** - подробное руководство по новой системе
- **`CHANGELOG_LOGGING_V2.md`** - этот файл (список изменений)

#### Скрипты:
- **`analyze_successful_deliveries.py`** - анализ логов успешных доставок

#### Обновленные:
- **`TRAINING_GUIDE.md`** - обновлен для отражения новой системы

---

## 📊 Формат логов

### Имя файла (новое)

```
success_delivery_<timestamp>_score<очки>_frames<кадры>.json
```

**Примеры:**
```
success_delivery_1730645123456_score250_frames145.json
success_delivery_1730645234567_score180_frames98.json
```

### Структура файла (новое)

```json
{
  "metadata": {
    "timestamp": 1730645123456,
    "date": "2024-11-03T15:25:23.456Z",
    "scoreEarned": 250,
    "totalFrames": 145,
    "duration": "7.3 seconds",
    "playerId": "player-12345",
    "successful": true,
    "description": "Successful orb delivery from pickup to deposit"
  },
  "frames": [
    {
      "x": 1500,
      "y": 300,
      "velX": 1.2,
      "velY": -0.5,
      ...
    }
  ]
}
```

### Старый формат (v1.0)

```
player_training_<timestamp>.json
```

Содержал только массив кадров без метаданных и структуры.

---

## 🚀 Как использовать

### Для игроков

**Просто играйте!** 🎮

1. Запустите игру: `.\restart-local.ps1`
2. Берите орбы и доставляйте на базы
3. Логи создаются автоматически при каждой успешной доставке

### Для разработчиков ML

1. **Соберите данные** (играя):
   ```bash
   # Проверьте количество файлов
   ls logs/success_delivery_*.json
   ```

2. **Проанализируйте**:
   ```bash
   python analyze_successful_deliveries.py
   ```

3. **Используйте для обучения**:
   ```bash
   # Behavioral Cloning
   python ml_bot/training/train_bc.py --logs-dir logs
   
   # Или PPO (Reinforcement Learning)
   python ml_bot/training/train_ppo.py
   ```

---

## 📈 Преимущества новой системы

### Для ML обучения

✅ **Качество данных**
- Только успешные маневры
- Нет "шума" от неудачных попыток
- Четкие эпизоды (начало → конец)

✅ **Структурированность**
- Каждый файл = один эпизод
- Метаданные для фильтрации
- Легко батчить

✅ **Анализ**
- Быстро найти лучшие примеры
- Фильтровать по качеству (score)
- Сортировать по длительности

### Для пользователей

✅ **Удобство**
- Не нужно помнить про клавиши
- Работает в фоне
- Автоматическая очистка "мусора"

✅ **Прозрачность**
- Видно сколько успешных доставок
- Видно качество (по score в имени)
- Понятная структура

---

## 🔄 Обратная совместимость

### Старые логи (`player_training_*.json`)

- ✅ Продолжат работать
- ✅ Можно использовать для обучения
- ⚠️ Но содержат смешанные данные (успешные + неудачные)

### Рекомендация

Собрать новые данные с автоматической системой для лучшего качества.

---

## 🐛 Известные ограничения

### Текущие

1. Клавиша `L` больше не используется (можно удалить в будущем)
2. Старый метод `_saveLogData()` остался для совместимости (не используется)

### Планы на будущее

- [ ] Добавить опциональную фильтрацию по минимальному score
- [ ] Сохранять траекторию орба для визуализации
- [ ] Добавить метки качества маневра (smooth/rough)
- [ ] Экспорт в формат для популярных ML библиотек

---

## 📊 Статистика изменений

### Код

- **Строк добавлено**: ~150
- **Строк удалено**: ~20
- **Новых методов**: 1 (`_saveSuccessfulDeliveryLog`)
- **Измененных методов**: 3 (`carryOrb`, `crash`, `deposit`)

### Документация

- **Новых файлов**: 2 (README + CHANGELOG)
- **Обновленных файлов**: 1 (TRAINING_GUIDE.md)
- **Новых скриптов**: 1 (analyze_successful_deliveries.py)

---

## 🎓 Обучение

### Минимальные требования

- **20-30 успешных доставок**
- **~2000-3000 кадров**
- **~2-3 минуты игры**

### Оптимальные требования

- **100+ успешных доставок**
- **10,000+ кадров**
- **~10 минут игры**

---

## 💡 Примеры использования

### Анализ данных

```bash
# Простая статистика
python analyze_successful_deliveries.py

# С экспортом объединенного датасета
python analyze_successful_deliveries.py --export

# Указать другую папку с логами
python analyze_successful_deliveries.py --logs-dir ../server/logs
```

### Обучение модели

```bash
# Behavioral Cloning (быстро)
cd ml_bot
python training/train_bc.py --logs-dir ../logs --epochs 100

# PPO (качественно)
python training/train_ppo.py --timesteps 2000000 --envs 8
```

---

## 🙏 Благодарности

Спасибо всем тестерам и игрокам, которые помогли собрать данные!

---

## 📞 Поддержка

Если возникли вопросы или проблемы:

1. Проверьте **SUCCESS_LOGGING_README.md**
2. Посмотрите **TRAINING_GUIDE.md**
3. Запустите `python analyze_successful_deliveries.py` для диагностики

---

**Версия**: 2.0  
**Дата**: 2024-11-03  
**Статус**: ✅ Готово к использованию

