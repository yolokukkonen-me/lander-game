# 🌐 Удалённое управление сервером

Скрипты для управления игровым сервером на **http://51.250.30.92/**

---

## 🚀 Быстрые команды

### Управление игровым сервером

```powershell
.\remote-status.ps1     # Проверить статус
.\remote-start.ps1      # Запустить сервер
.\remote-stop.ps1       # Остановить сервер
.\remote-restart.ps1    # Перезапустить сервер
.\remote-logs.ps1       # Посмотреть логи
```

### Управление Nginx

```powershell
.\remote-nginx.ps1 -Action status    # Статус Nginx
.\remote-nginx.ps1 -Action restart   # Перезапустить Nginx
.\remote-nginx.ps1 -Action reload    # Перезагрузить конфиг
```

### Прямой доступ

```powershell
.\remote-ssh.ps1        # SSH подключение к серверу
.\deploy.ps1            # Деплой новой версии
```

---

## 📋 Подробное описание команд

### `remote-status.ps1` - Проверка статуса

Показывает полную информацию о состоянии сервера:

```powershell
.\remote-status.ps1
```

**Что показывает:**
- ✅ Статус systemd сервиса (running/stopped)
- 🔌 Статус порта 3000
- 📊 Использование CPU и памяти
- 📝 Последние 5 строк логов
- 🌐 HTTP доступность

**Пример вывода:**
```
=== Service Status ===
● lander.service - Lander Game Server
     Loaded: loaded
     Active: active (running) since...
     Memory: 120.2M
     CPU: 46.939s

=== Port Status ===
tcp   LISTEN   *:3000   users:(("node",pid=665))

SERVER: RUNNING ✓
```

---

### `remote-start.ps1` - Запуск сервера

Запускает игровой сервер через systemd:

```powershell
.\remote-start.ps1
```

**Что делает:**
1. Проверяет SSH подключение
2. Запускает `systemctl start lander`
3. Ждёт 2 секунды
4. Проверяет статус
5. Показывает URL игры

**Когда использовать:**
- После остановки сервера
- После перезагрузки VM
- Если сервис упал

---

### `remote-stop.ps1` - Остановка сервера

Останавливает игровой сервер:

```powershell
.\remote-stop.ps1
```

**Когда использовать:**
- Перед обслуживанием сервера
- Для экономии ресурсов
- Перед ручным деплоем

⚠️ **Внимание:** Игра станет недоступна!

---

### `remote-restart.ps1` - Перезапуск сервера

Перезапускает сервер (останавливает и запускает заново):

```powershell
.\remote-restart.ps1
```

**Когда использовать:**
- После изменения конфигурации
- При подвисании сервера
- После ручного обновления файлов
- Если нужно применить изменения

**Downtime:** ~3 секунды

---

### `remote-logs.ps1` - Просмотр логов

Показывает логи игрового сервера:

```powershell
# Показать последние 50 строк (по умолчанию)
.\remote-logs.ps1

# Показать последние 100 строк
.\remote-logs.ps1 -Lines 100

# Показать последние 20 строк
.\remote-logs.ps1 -Lines 20

# Следить за логами в реальном времени (Ctrl+C для выхода)
.\remote-logs.ps1 -Follow
```

**Что показывает:**
- Ошибки игрового сервера
- Подключения игроков
- Сетевые события
- Ошибки Node.js

**Примеры использования:**

```powershell
# Отладка ошибки
.\remote-logs.ps1 -Lines 200

# Мониторинг в реальном времени
.\remote-logs.ps1 -Follow
```

---

### `remote-ssh.ps1` - Прямой SSH доступ

Подключается к серверу через SSH:

```powershell
.\remote-ssh.ps1
```

После подключения доступны команды:

```bash
# Управление сервисом
sudo systemctl status lander
sudo systemctl restart lander
sudo systemctl stop lander
sudo systemctl start lander

# Логи
sudo journalctl -u lander -f    # Следить за логами
sudo journalctl -u lander -n 100  # Последние 100 строк

# Файлы
cd /opt/lander
ls -la
nano examples/50-lander_virtual_keyboard/client.js

# Бэкапы
ls -la /opt/ | grep backup

# Выход
exit
```

---

### `remote-nginx.ps1` - Управление Nginx

Управляет веб-сервером Nginx:

```powershell
# Проверить статус
.\remote-nginx.ps1 -Action status

# Перезапустить (полный рестарт)
.\remote-nginx.ps1 -Action restart

# Перезагрузить конфигурацию (без downtime)
.\remote-nginx.ps1 -Action reload

# Остановить
.\remote-nginx.ps1 -Action stop

# Запустить
.\remote-nginx.ps1 -Action start
```

**Когда использовать:**

- **`status`** - проверить работу Nginx
- **`restart`** - после критических изменений
- **`reload`** - после изменения конфигурации (рекомендуется)
- **`stop/start`** - для обслуживания

---

## 🔧 Типичные сценарии

### Сценарий 1: Проверка работы сервера

```powershell
# 1. Проверяем статус
.\remote-status.ps1

# 2. Если всё ОК - открываем игру
start http://51.250.30.92/examples/50-lander_virtual_keyboard/

# 3. Если есть проблемы - смотрим логи
.\remote-logs.ps1 -Lines 50
```

---

### Сценарий 2: Сервер не отвечает

```powershell
# 1. Проверяем статус
.\remote-status.ps1

# 2. Смотрим логи на ошибки
.\remote-logs.ps1 -Lines 100

# 3. Перезапускаем сервер
.\remote-restart.ps1

# 4. Проверяем снова
.\remote-status.ps1

# 5. Открываем игру
start http://51.250.30.92/examples/50-lander_virtual_keyboard/
```

---

### Сценарий 3: Деплой с проверкой

```powershell
# 1. Проверяем текущий статус
.\remote-status.ps1

# 2. Деплоим новую версию
.\deploy.ps1

# 3. Ждём ~2 минуты

# 4. Проверяем что всё работает
.\remote-status.ps1

# 5. Смотрим логи на ошибки
.\remote-logs.ps1 -Lines 30

# 6. Если ОК - тестируем в браузере
start http://51.250.30.92/examples/50-lander_virtual_keyboard/
```

---

### Сценарий 4: Изменение конфигурации Nginx

```powershell
# 1. Подключаемся по SSH
.\remote-ssh.ps1

# 2. Редактируем конфиг
sudo nano /etc/nginx/sites-available/lander

# 3. Проверяем синтаксис
sudo nginx -t

# 4. Перезагружаем (без downtime)
sudo systemctl reload nginx

# 5. Выходим
exit

# 6. Или используем скрипт
.\remote-nginx.ps1 -Action reload
```

---

### Сценарий 5: Откат на предыдущую версию

```powershell
# 1. Подключаемся
.\remote-ssh.ps1

# 2. Смотрим доступные бэкапы
ls -la /opt/ | grep lander.backup

# 3. Останавливаем сервер
sudo systemctl stop lander

# 4. Откатываемся (замените дату!)
sudo rm -rf /opt/lander
sudo mv /opt/lander.backup.20251102_120000 /opt/lander

# 5. Запускаем
sudo systemctl start lander

# 6. Выходим
exit

# 7. Проверяем
.\remote-status.ps1
```

---

### Сценарий 6: Мониторинг в реальном времени

```powershell
# Терминал 1: Статус
while ($true) {
    Clear-Host
    .\remote-status.ps1
    Start-Sleep -Seconds 10
}

# Терминал 2: Логи
.\remote-logs.ps1 -Follow
```

---

## 🚨 Устранение проблем

### Проблема: "SSH connection failed"

```powershell
# Проверьте SSH ключ
Test-Path "$env:USERPROFILE\.ssh\yc"

# Проверьте подключение
ssh -i "$env:USERPROFILE\.ssh\yc" ubuntu@51.250.30.92 "echo OK"

# Проверьте Security Group в Yandex.Cloud (порт 22)
```

---

### Проблема: "Service is not running"

```powershell
# Запустите сервис
.\remote-start.ps1

# Если не помогло - смотрите логи
.\remote-logs.ps1 -Lines 100

# Если ошибка в коде - откатитесь на backup
.\remote-ssh.ps1
# ... откат на backup
```

---

### Проблема: "HTTP not responding"

```powershell
# 1. Проверьте Nginx
.\remote-nginx.ps1 -Action status

# 2. Перезапустите Nginx
.\remote-nginx.ps1 -Action restart

# 3. Проверьте игровой сервер
.\remote-status.ps1

# 4. Проверьте порт 80 в Security Group
```

---

### Проблема: "High CPU/Memory usage"

```powershell
# 1. Проверьте статус
.\remote-status.ps1

# 2. Перезапустите сервер
.\remote-restart.ps1

# 3. Если не помогло - подключитесь
.\remote-ssh.ps1

# 4. Проверьте процессы
top
ps aux | grep node

# 5. Перезагрузите VM (крайний случай)
sudo reboot
```

---

## 📊 Мониторинг

### Быстрая проверка здоровья

```powershell
# Статус сервера
.\remote-status.ps1

# Проверка HTTP
Invoke-WebRequest -Uri "http://51.250.30.92/examples/50-lander_virtual_keyboard/" -UseBasicParsing

# Проверка WebSocket (в браузере F12 → Console)
# Должны быть сообщения: "Connecting to server..." → "Connected"
```

---

### Регулярный мониторинг

Создайте задачу в Task Scheduler для автоматической проверки:

```powershell
# Скрипт проверки (save as check-health.ps1)
$status = & .\remote-status.ps1
if ($status -notlike "*RUNNING*") {
    Send-MailMessage -To "admin@example.com" -Subject "Server Down" -Body $status
}
```

---

## 🔐 Безопасность

### Лучшие практики

✅ **Используйте SSH ключи** (уже настроено)
✅ **Не делайте force push в Git**
✅ **Создавайте бэкапы перед изменениями** (deploy.ps1 делает автоматически)
✅ **Проверяйте логи после деплоя**
✅ **Тестируйте локально перед деплоем**

---

## 📞 Быстрая справка

| Задача | Команда |
|--------|---------|
| Проверить статус | `.\remote-status.ps1` |
| Перезапустить игру | `.\remote-restart.ps1` |
| Посмотреть логи | `.\remote-logs.ps1` |
| SSH подключение | `.\remote-ssh.ps1` |
| Деплой | `.\deploy.ps1` |
| Nginx рестарт | `.\remote-nginx.ps1 -Action restart` |

---

## 🔗 Ссылки

- **Игра:** http://51.250.30.92/examples/50-lander_virtual_keyboard/
- **CI/CD Guide:** [CI-CD-SETUP.md](CI-CD-SETUP.md)
- **Local Dev:** [LOCAL-DEV-GUIDE.md](LOCAL-DEV-GUIDE.md)
- **README:** [README.md](README.md)

---

**Управляйте сервером с лёгкостью! 🚀**

