# 🤖 Руководство по обучению бота

## 📝 Система логирования игрока (v2.0 - Автоматическая)

Для обучения бота используется **автоматическая** система записи действий реального игрока. Система логирует **только успешные доставки орбов** - от момента подъема орба до момента доставки на базу. Все данные сохраняются в JSON формате.

---

## 🎮 Как записать тренировочные данные

### ⚡ ВАЖНО: Логирование полностью автоматическое!

**Никаких клавиш нажимать не нужно!** Просто играйте нормально.

### Шаг 1: Запустите игру
1. Запустите локальный сервер: `.\restart-local.ps1`
2. Откройте игру в браузере: `http://localhost/examples/50-lander_virtual_keyboard/`

### Шаг 2: Играйте и доставляйте орбы
- Берите орб → **логирование начинается автоматически**
- Летите к платформе → **все действия записываются**
- Приземляйтесь → **данные сохраняются в отдельный файл**

**Если разобьетесь с орбом** → логи удаляются автоматически (неудачная попытка)

### Шаг 3: Проверьте результат
В консоли сервера вы увидите:
```
[TRAINING] Started automatic logging for orb pickup (client: player-12345)
[TRAINING] ✅ Saved SUCCESSFUL delivery: 145 frames, score: 250 -> success_delivery_1730645123456_score250_frames145.json
```

---

## 📊 Формат данных

### Имя файла
```
success_delivery_<timestamp>_score<очки>_frames<кадры>.json
```

Примеры:
- `success_delivery_1730645123456_score250_frames145.json`
- `success_delivery_1730645234567_score180_frames98.json`

### Структура файла

Каждый файл содержит:
- **metadata** - информация о доставке (время, очки, длительность)
- **frames** - массив кадров с данными игрока

### Данные в каждом кадре

### Физика и позиция
- `x`, `y` - координаты корабля
- `angle` - угол поворота корабля (радианы)
- `velX`, `velY` - линейная скорость по X и Y
- `angVel` - угловая скорость

### Состояние игрока
- `fuel` - текущее топливо (0-100)
- `score` - текущий счет
- `carryingOrb` - несет ли игрок орб (true/false)
- `landed` - приземлился ли корабль (true/false)

### Управление
- `left` - нажата ли левая клавиша (поворот влево)
- `right` - нажата ли правая клавиша (поворот вправо)
- `thrust` - нажата ли клавиша тяги (ускорение)

### Ближайший орб (цель)
- `orbDist` - расстояние до ближайшего орба
- `orbAngle` - угол к орбу относительно носа корабля
- `orbX`, `orbY` - координаты орба

### Ближайшая платформа (для посадки)
- `padDist` - расстояние до ближайшей платформы
- `padAngle` - угол к платформе относительно носа корабля
- `padX`, `padY` - координаты платформы

---

## 📁 Где хранятся данные

Файлы сохраняются в папке:
```
C:\OpenServer\domains\yolo-ide-isogenic-keys-dev.ru\examples\50-lander_virtual_keyboard\logs\
```

Каждый файл = одна успешная доставка орба!

---

## 🔧 Автоматическое сохранение

Данные автоматически сохраняются **при каждой успешной доставке орба**:
- ✅ Игрок поднял орб → начинается логирование
- ✅ Игрок доставил на платформу → файл сохраняется
- ❌ Игрок разбился с орбом → логи удаляются без сохранения

**Никаких действий от игрока не требуется!**

---

## 📈 Анализ собранных данных

### Быстрая проверка

```bash
# Подсчитать успешные доставки
ls logs/success_delivery_*.json | measure

# Или в PowerShell
(Get-ChildItem logs/success_delivery_*.json).Count
```

### Детальный анализ

```bash
python analyze_successful_deliveries.py
```

Скрипт покажет:
- Количество успешных доставок
- Общее количество кадров
- Среднюю длительность доставки
- Статистику по очкам
- Топ-5 лучших доставок
- Рекомендации по качеству данных

### Объединение всех данных

```bash
python analyze_successful_deliveries.py --export
```

Создаст файл `combined_successful_deliveries.json` со всеми кадрами из всех доставок

---

## 🎯 Как использовать данные для обучения

### 1. Соберите разные сценарии
- Запишите несколько сессий игры
- Включайте разные ситуации: взлет, посадка, маневры, сбор орбов
- Чем больше данных, тем лучше

### 2. Анализ данных
Откройте JSON файл и изучите:
- Как меняется угол корабля при приближении к цели
- Когда включается/выключается тяга
- Какая скорость при посадке
- Как управляются повороты

### 3. Настройка бота (BotPlayer.js)
На основе собранных данных можно настроить:

#### PID контроллеры (`_pidConfig`)
```javascript
angle: { kp: 0.8, ki: 0, kd: 0.2, deadband: 0.1 }
altitude: { kp: 0.15, ki: 0, kd: 0.3, deadband: 5 }
speed: { kp: 0.2, ki: 0, kd: 0.05, deadband: 0.15 }
```

#### Скорости (`_flightSpeeds`)
```javascript
cruise: 2.0,      // Крейсерская скорость
approach: 1.2,    // Скорость подхода к орбу
landing: 0.8      // Скорость посадки
```

#### Высоты полета
```javascript
SAFE_FLIGHT_ALTITUDE: 120  // Базовая безопасная высота
```

---

## 🔍 Пример анализа

### Посадка на платформу
Проанализируйте последние записи перед `landed: true`:
```json
{
  "velY": 0.8,     // Вертикальная скорость при касании
  "angle": 0.05,   // Почти вертикально
  "thrust": false, // Тяга выключена перед посадкой
  "padDist": 5     // Расстояние до платформы
}
```

### Подход к орбу
Найдите момент захвата орба (`carryingOrb: false → true`):
```json
{
  "orbDist": 15,   // На каком расстоянии захватывается
  "velX": 0.5,     // Горизонтальная скорость
  "velY": 1.2,     // Вертикальная скорость
  "orbAngle": 0.1  // Угол подхода
}
```

---

## 🚀 Дополнительные клавиши

- **L** - Включить/выключить логирование
- **I** - Режим бога (неуязвимость)
- **G** - Спавн тестовых орбов

---

## ⚙️ Настройка логирования

В файле `Player.js`:

```javascript
self._logInterval = 3; // Логировать каждые N фреймов
```

Чем меньше значение, тем больше данных, но и больше файл.

---

## 📈 Рекомендации

### Для начала (Minimum Viable Dataset)
- **20-30 успешных доставок** (файлов)
- **~2000-3000 кадров** всего
- **~2-3 минуты игрового времени**

### Для хорошего качества
- **100+ успешных доставок**
- **10,000+ кадров**
- **~10 минут игрового времени**

### Для отличной модели
- **500+ успешных доставок**
- **50,000+ кадров**
- **Разнообразие**: разные платформы, разные расстояния, разные маневры

### Общие советы
1. **Играйте естественно** - бот учится на ваших реальных действиях
2. **Качество > количество** - лучше 50 успешных доставок, чем 200 попыток с крэшами
3. **Разнообразие** - старайтесь доставлять орбы с разных позиций
4. **Не торопитесь** - плавные маневры лучше для обучения чем резкие

---

## 🛠️ Техническая информация

### Частота записи
- Интервал: каждые 3 фрейма
- При 60 FPS: ~20 записей в секунду
- 1000 записей ≈ 50 секунд игры

### Размер файлов
- ~500 байт на запись
- 1000 записей ≈ 500 KB
- 10 минут игры ≈ 6 MB

---

## 🆕 Что нового в версии 2.0

### Автоматизация
- ✅ Логирование начинается автоматически при подъеме орба
- ✅ Логи сохраняются автоматически при успешной доставке
- ✅ Логи удаляются автоматически при крэше
- ❌ Убрано ручное управление (клавиша L теперь не используется)

### Качество данных
- ✅ Только успешные маневры
- ✅ Каждый файл = один полный цикл (pickup → delivery)
- ✅ Метаданные в каждом файле (score, duration, frames)
- ✅ Понятные имена файлов с метриками

### Удобство
- ✅ Не нужно помнить про клавиши
- ✅ Легко анализировать (один файл = одна доставка)
- ✅ Готовые скрипты для анализа
- ✅ Подробная документация

### Миграция со старой версии

Старые логи (`player_training_*.json`) продолжат работать, но:
- Они содержат смешанные данные (успешные + неудачные)
- Нет структуры по эпизодам
- Нет метаданных

**Рекомендуется**: собрать новые данные с автоматической системой для лучшего качества обучения.

---

## 📚 Дополнительная документация

- **SUCCESS_LOGGING_README.md** - подробное описание новой системы логирования
- **analyze_successful_deliveries.py** - скрипт для анализа данных
- **ml_bot/** - папка с ML компонентами для обучения бота

---

Удачи в обучении бота! 🎮🤖

