# ⚡ Быстрый старт: Деплой изменений

## 🎯 Самый простой способ (1 команда!)

```powershell
cd C:\OpenServer\domains\yolo-ide-isogenic-keys-dev.ru
.\deploy.ps1
```

**Готово!** Через 2-3 минуты изменения будут на сервере.

---

## 📋 Пошаговая инструкция

### 1️⃣ Вносите изменения в коде

Редактируйте файлы в:
- `examples/50-lander_virtual_keyboard/` - клиентская логика
- `server/` - серверная логика
- `engine/` - движок (осторожно!)

### 2️⃣ Тестируете локально (опционально)

```powershell
cd server
node ige.js -g ../examples/50-lander_virtual_keyboard
```

Открыть: http://localhost:3000

### 3️⃣ Деплой на сервер

```powershell
.\deploy.ps1
```

Скрипт автоматически:
- ✅ Проверит SSH соединение
- 📦 Скопирует файлы
- 🔄 Перезапустит сервер
- ✅ Проверит статус

### 4️⃣ Проверяете результат

Откройте: http://51.250.30.92/examples/50-lander_virtual_keyboard/

**Не забудьте очистить кэш: Ctrl+Shift+R**

---

## 🔧 Что изменить в вашем случае

### Изменить gameClasses (игровая логика)

```powershell
# 1. Например, редактируем Player.js
code examples/50-lander_virtual_keyboard/gameClasses/Player.js

# 2. Деплоим
.\deploy.ps1

# 3. Готово!
```

### Изменить виртуальную клавиатуру

```powershell
# 1. Редактируем virtualjoystick.js
code examples/50-lander_virtual_keyboard/virtualjoystick.js

# 2. Или client.js (где она используется)
code examples/50-lander_virtual_keyboard/client.js

# 3. Деплоим
.\deploy.ps1
```

### Изменить серверную логику

```powershell
# 1. Редактируем server.js
code examples/50-lander_virtual_keyboard/server.js

# 2. Деплоим
.\deploy.ps1
```

---

## 🚨 Если что-то пошло не так

### Откат на предыдущую версию

```powershell
ssh -i "$env:USERPROFILE\.ssh\yc" ubuntu@51.250.30.92 @"
# Смотрим бэкапы
ls -la /opt/ | grep lander.backup

# Откатываемся (замените дату!)
sudo systemctl stop lander
sudo rm -rf /opt/lander
sudo mv /opt/lander.backup.20251102_120000 /opt/lander
sudo systemctl start lander
"@
```

### Проверить логи ошибок

```powershell
ssh -i "$env:USERPROFILE\.ssh\yc" ubuntu@51.250.30.92 "sudo journalctl -u lander -n 100"
```

### Перезапустить сервер вручную

```powershell
ssh -i "$env:USERPROFILE\.ssh\yc" ubuntu@51.250.30.92 "sudo systemctl restart lander && sudo systemctl status lander"
```

---

## 📊 Частые сценарии

### Быстрое исправление бага
```powershell
# 1. Исправили код
# 2. Деплой
.\deploy.ps1
# 3. Готово! (~2 мин)
```

### Добавление новой функции
```powershell
# 1. Написали код
# 2. Протестировали локально
cd server
node ige.js -g ../examples/50-lander_virtual_keyboard

# 3. Всё работает? Деплой!
.\deploy.ps1
```

### Обновление зависимостей
```powershell
# 1. Обновили package.json
code server/package.json

# 2. Деплой (автоматически установит зависимости)
.\deploy.ps1
```

---

## ⚙️ Настройка (если нужно)

### Изменить IP сервера

Отредактируйте `deploy.ps1`:
```powershell
$Server = "НОВЫЙ_IP"
```

### Изменить путь к SSH ключу

Отредактируйте `deploy.ps1`:
```powershell
$KeyPath = "C:\путь\к\ключу"
```

---

## 🎓 Дополнительная документация

- **Полная инструкция:** [CI-CD-SETUP.md](CI-CD-SETUP.md)
- **Основная документация:** [README.md](README.md)

---

**Нужна помощь?** Проверьте логи или откатитесь на предыдущую версию!

