# 📜 Справочник по всем скриптам проекта

Полный список всех скриптов управления для локальной разработки и удалённого сервера.

---

## 🏠 ЛОКАЛЬНЫЕ СКРИПТЫ (Разработка)

### `start-local.ps1` - Запуск локального сервера

```powershell
.\start-local.ps1
```

**Что делает:**
- Запускает Node.js сервер на порту 3000
- Открывает игру в браузере
- Сервер работает в отдельном окне PowerShell

**После запуска:**
- Игра доступна: http://localhost:3000/examples/50-lander_virtual_keyboard/
- Логи видны в окне сервера
- Закройте окно для остановки сервера

---

### `stop-local.ps1` - Остановка локального сервера

```powershell
.\stop-local.ps1
```

**Что делает:**
- Находит процесс Node.js на порту 3000
- Завершает процесс
- Освобождает порт

**Когда использовать:**
- Перед запуском новой версии
- Если сервер завис
- Для освобождения порта 3000

---

### `restart-local.ps1` - Перезапуск локального сервера

```powershell
.\restart-local.ps1
```

**Что делает:**
1. Останавливает текущий сервер
2. Ждёт 2 секунды
3. Запускает сервер заново
4. Открывает игру в браузере

**Когда использовать:**
- После изменения серверного кода
- Для быстрого перезапуска
- Если нужно применить изменения

---

### `status-local.ps1` - Проверка статуса локального сервера

```powershell
.\status-local.ps1
```

**Показывает:**
- ✅ Работает ли сервер на порту 3000
- 🔌 PID процесса Node.js
- 💾 Использование памяти
- 🖥️ Использование CPU
- 📊 Статус порта 3000

**Пример вывода:**
```
=== Port 3000 Status ===
Status: LISTENING
Process: node (PID: 12345)
Memory: 120 MB
CPU: 2.5%

SERVER: RUNNING ✓
Game URL: http://localhost:3000/examples/50-lander_virtual_keyboard/
```

---

### `logs-local.ps1` - Логи локального сервера

```powershell
# Показать последние 50 строк
.\logs-local.ps1

# Показать последние 100 строк
.\logs-local.ps1 -Lines 100

# Следить в реальном времени (Ctrl+C для выхода)
.\logs-local.ps1 -Follow
```

**Показывает:**
- Вывод console.log() из серверного кода
- Ошибки Node.js
- Подключения игроков
- Сетевые события

⚠️ **Примечание:** Логи доступны только если сервер был запущен через `start-local.ps1`

---

## 🌐 УДАЛЁННЫЕ СКРИПТЫ (Продакшн сервер)

Все скрипты работают с сервером **51.250.30.92**

---

### `remote-status.ps1` - Статус удалённого сервера

```powershell
.\remote-status.ps1
```

**Показывает:**
- ✅ Статус systemd сервиса
- 🔌 Порт 3000 (listening/not)
- 💾 Memory usage
- 🖥️ CPU usage
- 📝 Последние 5 строк логов
- 🌐 HTTP доступность (код 200)

**Пример вывода:**
```
=== Service Status ===
● lander.service - Lander Game Server
     Active: active (running)
     Memory: 31.8M
     CPU: 1.332s

=== Port Status ===
tcp   LISTEN   *:3000   users:(("node",pid=5483))

SERVER: RUNNING ✓
http://51.250.30.92/examples/50-lander_virtual_keyboard/
```

---

### `remote-start.ps1` - Запуск удалённого сервера

```powershell
.\remote-start.ps1
```

**Что делает:**
1. Подключается по SSH
2. Запускает `systemctl start lander`
3. Ждёт 2 секунды
4. Проверяет статус

**Когда использовать:**
- После остановки сервера
- После перезагрузки VM
- Если сервис упал

---

### `remote-stop.ps1` - Остановка удалённого сервера

```powershell
.\remote-stop.ps1
```

**Что делает:**
1. Подключается по SSH
2. Останавливает `systemctl stop lander`
3. Проверяет статус

⚠️ **Внимание:** Игра станет недоступна для всех пользователей!

**Когда использовать:**
- Перед обслуживанием сервера
- Для экономии ресурсов
- Перед ручным деплоем

---

### `remote-restart.ps1` - Перезапуск удалённого сервера

```powershell
.\remote-restart.ps1
```

**Что делает:**
1. Останавливает сервер
2. Ждёт 1 секунду
3. Запускает сервер
4. Проверяет статус

**Downtime:** ~3 секунды

**Когда использовать:**
- После изменения конфигурации
- При подвисании сервера
- После ручного обновления файлов
- Для применения изменений

---

### `remote-logs.ps1` - Логи удалённого сервера

```powershell
# Показать последние 50 строк (по умолчанию)
.\remote-logs.ps1

# Показать последние 100 строк
.\remote-logs.ps1 -Lines 100

# Следить в реальном времени (Ctrl+C для выхода)
.\remote-logs.ps1 -Follow
```

**Показывает:**
- Логи из journalctl
- Ошибки Node.js
- Подключения игроков
- Сетевые события
- Системные сообщения

**Примеры:**
```powershell
# Отладка ошибки
.\remote-logs.ps1 -Lines 200

# Мониторинг подключений
.\remote-logs.ps1 -Follow
```

---

### `remote-ssh.ps1` - SSH подключение к серверу

```powershell
.\remote-ssh.ps1
```

**Что делает:**
- Открывает SSH сессию на сервер
- Показывает полезные команды при подключении

**Доступные команды на сервере:**
```bash
# Управление сервисом
sudo systemctl status lander
sudo systemctl restart lander
sudo systemctl stop lander
sudo systemctl start lander

# Логи
sudo journalctl -u lander -f          # Следить
sudo journalctl -u lander -n 100      # Последние 100

# Файлы
cd /opt/lander
ls -la
nano examples/50-lander_virtual_keyboard/client.js

# Nginx
sudo systemctl status nginx
sudo nginx -t                         # Проверить конфиг
sudo systemctl reload nginx           # Без downtime

# Бэкапы
ls -la /opt/ | grep backup

# Выход
exit
```

---

### `remote-nginx.ps1` - Управление Nginx

```powershell
# Проверить статус
.\remote-nginx.ps1 -Action status

# Перезапустить (полный рестарт)
.\remote-nginx.ps1 -Action restart

# Перезагрузить конфигурацию (без downtime!)
.\remote-nginx.ps1 -Action reload

# Остановить
.\remote-nginx.ps1 -Action stop

# Запустить
.\remote-nginx.ps1 -Action start
```

**Когда использовать:**

| Action | Когда использовать | Downtime |
|--------|-------------------|----------|
| `status` | Проверить работу Nginx | Нет |
| `restart` | После критических изменений | Да (~1 сек) |
| `reload` | После изменения конфигурации | **Нет** |
| `stop` | Для обслуживания | Да |
| `start` | После остановки | Нет |

💡 **Рекомендация:** Используйте `reload` вместо `restart` при изменении конфигурации Nginx!

---

## 🚀 ДЕПЛОЙ СКРИПТЫ

### `deploy.ps1` - Деплой на продакшн сервер

```powershell
.\deploy.ps1
```

**Что делает:**
1. Проверяет SSH подключение
2. Создаёт резервную копию на сервере
3. Синхронизирует файлы через rsync:
   - `/engine` - движок игры
   - `/examples/50-lander_virtual_keyboard` - игра
   - `/server` - серверная часть
4. Устанавливает зависимости (`npm ci`)
5. Перезапускает игровой сервер
6. Проверяет статус

**Что НЕ деплоится:**
- `node_modules/` (устанавливаются на сервере)
- `.git/` (не нужно)
- `*.md` файлы (документация)
- Временные файлы

**Бэкапы:**
- Создаются автоматически: `/opt/lander.backup.YYYYMMDD_HHMMSS`
- Хранятся на сервере
- Можно откатиться вручную через SSH

**Время выполнения:** ~2-3 минуты

**После деплоя:**
```powershell
# Проверить статус
.\remote-status.ps1

# Проверить логи
.\remote-logs.ps1 -Lines 30

# Открыть игру (обновить через Ctrl+Shift+R!)
start http://51.250.30.92/examples/50-lander_virtual_keyboard/
```

---

## 🔧 ТИПИЧНЫЕ СЦЕНАРИИ

### Сценарий 1: Локальная разработка

```powershell
# 1. Запустить локальный сервер
.\start-local.ps1

# 2. Внести изменения в код

# 3. Перезапустить сервер
.\restart-local.ps1

# 4. Проверить логи
.\logs-local.ps1 -Lines 50

# 5. Остановить когда закончите
.\stop-local.ps1
```

---

### Сценарий 2: Деплой изменений

```powershell
# 1. Проверить что всё работает локально
.\start-local.ps1
# Тестируем игру

# 2. Остановить локальный сервер
.\stop-local.ps1

# 3. Деплой на продакшн
.\deploy.ps1

# 4. Подождать ~2 минуты

# 5. Проверить статус
.\remote-status.ps1

# 6. Проверить логи
.\remote-logs.ps1 -Lines 30

# 7. Тестировать (с Ctrl+Shift+R)
start http://51.250.30.92/examples/50-lander_virtual_keyboard/
```

---

### Сценарий 3: Удалённый сервер не отвечает

```powershell
# 1. Проверить статус
.\remote-status.ps1

# 2. Посмотреть логи
.\remote-logs.ps1 -Lines 100

# 3. Перезапустить сервер
.\remote-restart.ps1

# 4. Проверить снова
.\remote-status.ps1

# 5. Если не помогло - SSH
.\remote-ssh.ps1
# На сервере: sudo systemctl status lander
```

---

### Сценарий 4: Мониторинг в реальном времени

```powershell
# Терминал 1: Статус каждые 10 секунд
while ($true) {
    Clear-Host
    .\remote-status.ps1
    Start-Sleep -Seconds 10
}

# Терминал 2: Логи в реальном времени
.\remote-logs.ps1 -Follow
```

---

### Сценарий 5: Изменение конфигурации Nginx

```powershell
# 1. SSH подключение
.\remote-ssh.ps1

# 2. На сервере: редактировать конфиг
sudo nano /etc/nginx/sites-available/lander

# 3. Проверить синтаксис
sudo nginx -t

# 4. Перезагрузить (без downtime!)
sudo systemctl reload nginx
exit

# 5. Или используйте скрипт
.\remote-nginx.ps1 -Action reload
```

---

### Сценарий 6: Откат на предыдущую версию

```powershell
# 1. SSH подключение
.\remote-ssh.ps1

# 2. Посмотреть доступные бэкапы
ls -la /opt/ | grep lander.backup

# 3. Остановить сервер
sudo systemctl stop lander

# 4. Откатиться (замените дату!)
sudo rm -rf /opt/lander
sudo mv /opt/lander.backup.20251102_120000 /opt/lander

# 5. Запустить
sudo systemctl start lander
exit

# 6. Проверить
.\remote-status.ps1
```

---

## 📊 БЫСТРАЯ СПРАВКА

### Локальная разработка

| Задача | Команда |
|--------|---------|
| Запустить | `.\start-local.ps1` |
| Остановить | `.\stop-local.ps1` |
| Перезапустить | `.\restart-local.ps1` |
| Проверить статус | `.\status-local.ps1` |
| Посмотреть логи | `.\logs-local.ps1` |
| Игра | http://localhost:3000/examples/50-lander_virtual_keyboard/ |

### Удалённый сервер

| Задача | Команда |
|--------|---------|
| Проверить статус | `.\remote-status.ps1` |
| Запустить | `.\remote-start.ps1` |
| Остановить | `.\remote-stop.ps1` |
| Перезапустить | `.\remote-restart.ps1` |
| Логи | `.\remote-logs.ps1` |
| SSH | `.\remote-ssh.ps1` |
| Nginx | `.\remote-nginx.ps1 -Action <action>` |
| Деплой | `.\deploy.ps1` |
| Игра | http://51.250.30.92/examples/50-lander_virtual_keyboard/ |

---

## ⚙️ ПАРАМЕТРЫ СКРИПТОВ

### Настройка удалённых скриптов

Все удалённые скрипты принимают параметры:

```powershell
# Использовать другой сервер
.\remote-status.ps1 -Server "192.168.1.100" -User "admin" -KeyPath "C:\keys\my.key"

# Посмотреть больше логов
.\remote-logs.ps1 -Lines 200

# Следить за логами
.\remote-logs.ps1 -Follow

# Nginx reload
.\remote-nginx.ps1 -Action reload
```

---

## 🔐 SSH Ключи

Все скрипты используют SSH ключ: `C:\Users\ВАШ_ПОЛЬЗОВАТЕЛЬ\.ssh\yc`

Проверить наличие ключа:
```powershell
Test-Path "$env:USERPROFILE\.ssh\yc"
```

Если ключа нет, см. [CI-CD-SETUP.md](CI-CD-SETUP.md)

---

## 🚨 Устранение проблем

### "SSH connection failed"

```powershell
# Проверьте ключ
Test-Path "$env:USERPROFILE\.ssh\yc"

# Проверьте подключение
ssh -i "$env:USERPROFILE\.ssh\yc" ubuntu@51.250.30.92 "echo OK"
```

### "Port 3000 already in use" (локально)

```powershell
# Остановите процесс
.\stop-local.ps1

# Или вручную
Get-Process | Where-Object {$_.Path -like "*node.exe*"} | Stop-Process -Force
```

### "Service is not running" (удалённо)

```powershell
# Запустите сервис
.\remote-start.ps1

# Если не помогло - логи
.\remote-logs.ps1 -Lines 100
```

---

## 📚 Дополнительная документация

- **Полное руководство по удалённому управлению:** [REMOTE-MANAGEMENT.md](REMOTE-MANAGEMENT.md)
- **Локальная разработка:** [LOCAL-DEV-GUIDE.md](LOCAL-DEV-GUIDE.md)
- **CI/CD Setup:** [CI-CD-SETUP.md](CI-CD-SETUP.md)
- **Deployment:** [README-DEPLOYMENT.md](README-DEPLOYMENT.md)
- **Quick Start:** [QUICK-START.md](QUICK-START.md)

---

**Полный набор инструментов для комфортной разработки! 🚀**

