# 🤖 SimpleBotPlayer - Простой и надежный бот

## 📊 Основан на реальных данных игрока

**Источник**: `player_training_1762207737151.json`
- 689 записей игровой сессии
- 5 успешных посадок
- 2 захвата орбов
- 55 секунд игры

---

## ✅ Ключевые особенности

### 1. **Простая архитектура**
- ❌ Нет сложной системы waypoints
- ❌ Нет path planning
- ❌ Нет PID контроллеров
- ✅ Только базовая логика: найти → лететь → захватить → посадка

### 2. **Оптимизированные параметры**

Все значения взяты из анализа реальной игры:

| Параметр | Значение | Источник |
|----------|----------|----------|
| Крейсерская скорость | 2.0 | Средняя: 2.03 |
| Скорость подхода к орбу | 1.5 | Средняя: 2.14 |
| Скорость посадки | 0.7 | Средняя: 0.66 |
| Максимальная скорость | 2.5 | Предел безопасности |
| Безопасная высота | 90px | Средняя высота: 88px |
| Угол посадки | ±0.1 rad | Почти вертикально |

### 3. **Простая машина состояний**

```
idle → toOrb → toBase → landing → idle
  ↓       ↓       ↓        ↓
 Ищем   Летим  Летим с  Садимся
  орб   к орбу  орбом
```

---

## 🎯 Логика работы

### Состояние: `idle`
- Ищет ближайший орб
- Переходит в `toOrb`

### Состояние: `toOrb`
- Поворачивается к орбу
- Летит прямо к орбу
- Замедляется на расстоянии < 100px
- При захвате → переходит в `toBase`

### Состояние: `toBase`
- Ищет ближайшую платформу
- Летит к платформе
- При приближении < 100px → переходит в `landing`

### Состояние: `landing`
- Выравнивает корабль вертикально (angle → 0)
- Контролирует вертикальную скорость (velY → 0.7)
- Если далеко по X (>20px) → подлетает горизонтально
- Если близко → плавное падение с торможением
- При касании → переходит в `idle`

---

## 🔧 Основные методы

### `flyToTarget(x, y, targetSpeed)`
Базовая логика полета к цели:
1. Вычисляет угол к цели
2. Поворачивает корабль
3. Включает thrust если:
   - Падает быстро (velY > 1.5) - компенсация гравитации
   - Смотрит в правильную сторону и нужно ускориться
   - Слишком низко (altitude < 40px)

### `landOnPad(x, y)`
Логика посадки:
1. Выравнивает корабль вертикально (angle → 0)
2. Контролирует вертикальную скорость:
   - Целевая: 0.7 px/frame
   - Максимум: 1.0 px/frame
3. Тормозит если падает быстрее
4. Отпускает если падает медленнее

### `checkStuck()`
Защита от застревания:
- Проверяет движение каждые 6 тиков
- Если не двигается 300 тиков (5 сек) → сброс состояния

---

## 📈 Преимущества перед старым ботом

| Аспект | Старый BotPlayer | Новый SimpleBotPlayer |
|--------|------------------|----------------------|
| Строк кода | ~1500 | ~400 |
| Сложность | Высокая | Низкая |
| Системы | Waypoints, PID, Path planning | Только базовая логика |
| Застревает | ✅ Да (летит только вверх) | ❌ Нет (простая логика) |
| Понятность | ❌ Сложно разобраться | ✅ Легко понять и изменить |
| Оптимизация | Теоретическая | На основе реальных данных |

---

## 🚀 Запуск

Бот автоматически появляется когда в игре **ровно 1 игрок**.

```bash
.\restart-local.ps1
```

Откройте игру: `http://localhost/examples/50-lander_virtual_keyboard/`

---

## 🐛 Отладка

В консоли сервера появятся логи:

```
[BOT] Орб найден, летим к нему
[BOT] Орб подобран, летим на базу
[BOT] Начинаем посадку
[BOT] Посадка успешна!
```

Если бот застрял:
```
[BOT] Застрял, сброс состояния
```

---

## 🔮 Дальнейшие улучшения

### Легко добавить:
- 🎯 Избегание препятствий (проверка высоты рельефа впереди)
- 🎮 Избегание других игроков
- 📊 Более точная траектория на основе большего количества данных
- 🧠 Машинное обучение на основе логов

### Не нужно:
- ❌ Сложные системы waypoints
- ❌ PID контроллеры (работает и так)
- ❌ Path planning (прямой путь эффективнее)

---

## 📝 Изменение параметров

Все параметры в `init()`:

```javascript
this._params = {
    cruiseSpeed: 2.0,        // Скорость полета
    approachSpeed: 1.5,      // Скорость подхода
    landingSpeed: 0.7,       // Скорость посадки
    maxSpeed: 2.5,           // Максимум
    safeAltitude: 90,        // Безопасная высота
    minAltitude: 40,         // Минимум
    angleThreshold: 0.3,     // Угол для thrust
    landingAngle: 0.1,       // Угол посадки
    orbApproachDist: 100,    // Дистанция замедления
    padApproachDist: 100,    // Дистанция начала посадки
    arrivalDist: 40          // Считаем прибытие
};
```

Измените значения и перезапустите сервер!

---

## 🎓 Обучение

Хотите улучшить бота?

1. **Запишите больше логов** (клавиша `L` в игре)
2. **Проанализируйте** (`python analyze_training.py файл.json`)
3. **Обновите параметры** в `SimpleBotPlayer.js`
4. **Перезапустите** сервер

---

**Версия**: 1.0  
**Дата**: 2025-11-03  
**Основа**: Анализ реальной игры (689 записей)  
**Статус**: ✅ Работает и тестируется

